apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ebs-sc
provisioner: ebs.csi.aws.com
reclaimPolicy: Retain # Khi xóa Pod, giữ lại ổ đĩa để an toàn (hoặc Delete nếu muốn tự động xóa)
volumeBindingMode: WaitForFirstConsumer
parameters:
  type: gp3